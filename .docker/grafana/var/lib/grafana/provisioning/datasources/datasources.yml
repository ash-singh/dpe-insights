apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    url: ${DATASOURCE_HOST}:${DATASOURCE_PORT}
    database: ${DATASOURCE_DATABASE}
    user: ${DATASOURCE_USER}
    secureJsonData:
      password: ${DATASOURCE_PASSWORD}
